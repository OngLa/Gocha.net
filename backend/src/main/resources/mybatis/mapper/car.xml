<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kosa.afnica.backend.db.mapper.CarMapper">
    <!--  Brand 저장  -->
    <insert id="saveBrand" parameterType="brand">
        INSERT INTO BRAND (ID, LOGO, NAME)
        VALUES (#{id}, #{logo}, #{name})
    </insert>

    <!--  모든 Brand 조회  -->
    <select id="findAllBrand" resultType="brand">
        SELECT ID, NAME, LOGO
        FROM BRAND
    </select>

    <!--  CarType 저장  -->
    <insert id="saveCarType" parameterType="carType">
        INSERT INTO CAR_TYPE (ID, NAME, BRAND_ID, LOGO)
        VALUES (#{id}, #{name}, #{brandId}, #{logo})
    </insert>

    <!--  BrandId를 기반으로 모든 CarType 조회  -->
    <select id="findAllCarTypeByBrandId" parameterType="Long" resultType="carType">
        SELECT ID, NAME, BRAND_ID, LOGO
        FROM CAR_TYPE
        WHERE BRAND_ID = #{brandId}
    </select>

    <!--  내 차량 등록  -->
    <insert id="saveCar" parameterType="car">
        <selectKey keyProperty="id" resultType="Long" order="BEFORE">
            SELECT CAR_SEQ.nextval FROM DUAL
        </selectKey>
        INSERT INTO CAR (ID, CAR_NUMBER, CARTYPE_ID, MEMBER_ID)
        VALUES (#{id}, #{carNumber}, #{cartypeId}, #{memberId})
    </insert>

    <!--  MemberId를 기반으로 모든 CarId, CartypeName, Car Photo 조회  -->
    <select id="findAllCarByMemberId" parameterType="Long" resultType="carResDto">
        SELECT c.ID AS CAR_ID, ct.LOGO, ct.NAME AS CAR_NAME
        FROM CAR c
                 JOIN CAR_TYPE ct ON c.CARTYPE_ID = ct.ID
        WHERE c.MEMBER_ID = #{memberId}
    </select>

</mapper>