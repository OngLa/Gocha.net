<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kosa.afnica.backend.db.mapper.MemberMapper">

    <!--  멤버 회원가입  -->
    <insert id="save" parameterType="member">
        insert into member(id, email, password, name, phone_number, address, enabled, role)
        values (member_seq.nextval, #{email}, #{password}, #{name}, #{phoneNumber}, null, 1, 'ROLE_USER')
    </insert>

    <!--  카센터 회원가입(아직 구현x)  -->
    <insert id="saveAdmin" parameterType="member">
        insert into member(id, email, password, name, phone_number, address, enabled, role)
        values (carcenter_seq.nextval, #{email}, #{password}, #{name}, #{phoneNumber}, #{address}, 1, 'ROLE_CARCENTER')
    </insert>

    <!--  로그인 시 email에 대한 member객체 확인을 위해  -->
    <select id="findByEmail" parameterType="String" resultType="member">
        select id,
               email,
               password,
               name,
               phone_number,
               address,
               enabled,
               role
        from member
        where email = #{email}
    </select>

    <select id="findById" parameterType="Long" resultType="member">
        select id,
               email,
               password,
               name,
               phone_number,
               address,
               enabled,
               role
        from member
        where id = #{id}
    </select>

    <!--  FE에서 멤버 권한 확인하기 위해 Select Role  -->
    <select id="findRoleByEmail" parameterType="String" resultType="String">
        select role
        from member
        where email = #{email}
    </select>

    <!-- 이메일 중복 검사 -->
    <select id="existByEmail" resultType="int">
        select count(*)
        from member
        where name = #{name}
    </select>

    <!-- 닉네임 중복 검사 -->
    <select id="existByName" resultType="int">
        select count(*)
        from member
        where name = #{name}
    </select>

    <!--  비밀번호 수정  -->
    <update id="update" parameterType="member">
        update member
        set password=#{password}
        where id = #{id}
    </update>

</mapper>